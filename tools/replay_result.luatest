-- SPDX-License-Identifier: AGPL-3.0-only
-- Trusted standalone CLI harness. It has no PrusaSlicer API and performs no writes.
dimensional_accuracy_test = {}
dofile(arg[1])
local core = dimensional_accuracy_test
local opts = dofile(arg[2])
local requested_version = arg[3]
local plan = core.calculate_plan_version(opts, requested_version)
print(core.json_encode(plan))
